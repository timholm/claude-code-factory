You are building a production-grade open-source tool. Read SPEC.md for the full requirements.

## Your Task

Build a COMPLETE, WORKING implementation. Not a scaffold — real code that solves the stated problem.

## Requirements

- Implement ALL core functionality described in SPEC.md
- Write real tests (not placeholder tests) that exercise the core logic
- Run `make test` and ensure it passes before you stop
- Use idiomatic patterns for {{.Language}}
- Include proper error handling at system boundaries
- No TODOs, no placeholder functions, no "implement this later" comments
- No unnecessary abstractions — write straightforward code

## Project Structure

The workspace already has scaffolding:
- SPEC.md (your requirements — read it first)
- Makefile (build/test targets)
- Language-specific config files (go.mod, package.json, pyproject.toml, etc.)
- .gitignore
- LICENSE

Build on top of these files. Do not delete or overwrite them unless necessary.

## Quality Bar

The finished project should look like it was written by an experienced developer who cares about craft:
- Functions do one thing
- Variable names are clear
- Tests cover the important paths (happy path + key error cases)
- The code compiles/runs without errors

You have {{.MaxTurns}} turns. Use them all if needed. Quality over speed.
